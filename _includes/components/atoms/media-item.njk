<li class="media-item">
  <a href="{{ item.url }}">
    <div class="poster">
      <img class="media-cover"src="{{ item.cover }}" alt="{{item.title }} cover">
    </div>
    <div class="media-content">
      <p>{{ item.title }}</p>
      {% if item.author %}
        ({{ item.author }})
      {% elif item.director %}
        ({{ item.director }}, {{ item.released }})
      {% elif item.season %}
        (S{{ item.season }})
      {% elif item.platform %}
        ({{ item.platform }})
    {% endif %}
    </div>
  </a>
</li>
