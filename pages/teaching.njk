---
layout: layouts/default
title: Teaching
permalink: teaching/
---

<h2>Courses</h2>
<p>These are the courses I give at the Amsterdam University Communication and Multimedia Design</p>
  <ul class="course">
  {% for item in courses.courses %}
    <article class="project">
    <a href="{{ item.url }}">
      <img src="{{ item.image }}" alt="item Thubmail">
      <h3>{{ item.title }}</h3>
      <p>{{ item.description }}</p>
    </a>
  </article>
  {% endfor %}
  </ul>

<h2>Workshops</h2>
<p>I do workshops covering designing for the web for your conference or office.</p>
  <ul class="workshop">
  {% for item in workshops.workshops %}
    <article class="project">
    <a href="{{ item.url }}">
      <img src="{{ item.image }}" alt="item Thubmail">
      <h3>{{ item.title }}</h3>
      <p>{{ item.description }}</p>
    </a>
  </article>
  {% endfor %}
  </ul>

<h2>Online Classes</h2>
<p>Learn with me around the web.</p>
  <ul class="workshop">
  {% for item in classes.classes %}
    <article class="project">
    <a href="{{ item.url }}">
      <img src="{{ item.image }}" alt="item Thubmail">
      <h3>{{ item.title }}</h3>
      <p>{{ item.description }}</p>
    </a>
  </article>
  {% endfor %}
  </ul>
